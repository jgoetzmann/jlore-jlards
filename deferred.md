# Deferred cards (145)

### mediator  [needsEngine -> test/catalog-integrity.test.ts]
BLOCKING, and not only my cards. `tools/validate-cards.ts` was updated mid-session to allow the '$discovered'/'$selected' sentinels in a createCard/addToPileTop defId and a transform `into` (it passes now), but test/catalog-integrity.test.ts:156-178 still fails B97 for 13 cards using the sentinel — mediator, welfare, glubby_gloob_the_auctioneer (mine) plus kwzki_high_council_consultant, shadiris_visions, yuyas_embrace, mewing, cncias, jeweled_scarab, golden_scarab, empyreal_scarab, mathemagiks, homebrew, and model_citizen (transform). Mirror the tools/validate-cards.ts sentinel exemption into both B97 tests.

### the_big_backening  [needsEngine -> src/engine/core/buy.ts]
Left unedited. 'onBuy' is in SELF_EVENTS and buy.ts:186 fires it only on the bought instance (plus the Field), so a rider sitting in play can never see another card's purchase. The finding's alternative — a nextCardModifier with appendEffects — does not work either: peekBuyMods (buy.ts:35-48) reads only costDelta, costFloor and buyTo. Needs a non-self buy event or buy-side appendEffects; the copies then have to be of the bought defId and go back into the pile it came from. Card is currently a vanilla +1 Buy / +1 Money.

### new_banner_day  [needsEngine -> src/engine/core/buy.ts]
Left unedited. `{op:'nextCardModifier', mod:{appliesTo:'buy', buyTo:'gy', uses:1}}` is inert — 'gy' is already the default destination (buy.ts:166) — but it is harmless (consumeBuyMods decrements every buy mod, so it does not eat another card's discount). The printed rider needs a one-shot buy hook that remembers the discovered defId's cost and, on a purchase of a different card costing at least that much, runs `{op:'gainCard', from:{shop:'all', filter:{defId:'$discovered'}}, to:'gy', free:true}`.

### i_ship_it  [needsEngine -> src/engine/effects/ops/shop.ts]
Left unedited — the card data is already right. opMergePiles (shop.ts:250-276) pushes every card of the second pile onto the first, empties the source and fires onPileEmpty; nothing is shuffled and nothing is split, so a Draft slot goes to zero and counts toward emptyPileAbsolute/emptyPileFraction. Fix belongs in the op: concatenate, shuffle with the state rng, deal back alternately into both pile ids (rewriting each instance's pileId and splitting startingSize).

### nickel_and_dime  [needsEngine -> src/engine/core/triggers.ts]
Left unedited. fireOwnedTriggers (triggers.ts:152) builds candidates as `[...p.play, ...p.hand, ...p.gy, ...p.library]` — the trash is never enumerated, so a Flimsy card's onOpponentBuy can never fire and no `zones:['trash']` declaration can rescue it. Either sweep trashed instances for owned-trigger events, or give NextCardMod a payout field so both halves of the card resolve from player state. Dropping Flimsy would contradict the doc row.

### price_fixing  [needsEngine -> src/engine/effects/context.ts]
There is no EXPR_VAR for the average Draft pile cost, so `floor(avgCostOfDeck)` (the mean cost of the player's own library+hand+gy+play) stands in for it. Add e.g. `avgDraftPileCost` to EXPR_VARS and buildVars, computed with pileCost() over shop.order.draft, and swap the expression.

### cloud_nine  [needsEngine -> src/engine/effects/ops/shop.ts]
The 'remove all cost changes' half is unshippable today: opModifyCost only ever appends a CostMod. Either treat a `{op:'modifyCost'}` carrying neither `delta` nor `setTo` as a clear (empty pile.costMods, delete pile.costOverride, empty shop.globalCostMods), or add a dedicated reset op. I removed the delta-0 node meanwhile because its permanent floor-0 entry would have clamped every negative price in the match.

### supernova  [needsEngine -> src/engine/effects/ops/shop.ts]
Also covers chron_cache. `costOverride` on addToPileTop prices the whole PILE, permanently (costOf honours pile.costOverride with no expiry), so both cards' printed '(0)-cost Diamond' / 'they all cost (1)' clauses had to be dropped. Needs an instance-scoped price — a per-CardInstance cost override the cost stack reads — before those clauses can come back. chron_cache additionally needs an insert-at-index affordance ('replace the 2nd card') that addToPileTop does not have.

### bad_omen  [needsEngine -> src/engine/effects/ops/shop.ts]
'Buying from it now ALSO gives a Cursed Pig' cannot be expressed: a pile is an ordered stack, so anything added to the top is bought instead of the card under it. Needs a pile-scoped purchase rider (a CostMod-like entry that grants a card on buy). I retitled the card to what it actually does.

### cult_leader  [needsEngine -> src/engine/types.ts]
Also covers arm_of_the_jempire. 'a card you can currently afford' / '+1 Card for each pile you could have afforded' cannot be written: NumericFilter (types.ts:257-263) accepts literal numbers only, so `cost: {lte: <moneyUnspent>}` has no form. Either let NumericFilter take an Amount, or add a money-aware pile/pool predicate. Cult Leader currently offers 3 Draft Shop cards with no affordability gate; Arm of the Jempire draws min(3, moneyUnspent) as a proxy.

### freeze_tag  [needsEngine -> src/engine/effects/evaluate.ts]
`{ifPrevious: true}` reads `ctx.vars['__previousDidSomething']` (evaluate.ts:157-160) and grepping all of src/ finds exactly one occurrence of that key — the constant declaration. Nothing ever writes it, so the clause is always false; it is a dead engine feature, not just a dead card clause. Freeze Tag additionally wants a comparison of the two chosen piles' costs, which needs the pile ids threaded through the nodes or an expr var exposing them.

### pocket_wormhole  [needsEngine -> src/engine/effects/ops/choices.ts]
Also covers five_year_plan. Two gaps. (1) A Selector has no pile axis, so 'every card in one chosen pile' can only be written as 'every shop card with defId X' — which is why both cards now Discover the pile by its top card. (2) substituteDefId swaps BOTH '$discovered' and '$selected' for the same defId, so a selectCards nested around a discover cannot carry two different picks; that is what blocks the doc's literal reading ('choose a pile, THEN Discover a card to replace all copies with'). Make each sentinel belong to its own prompt kind and both cards become expressible; today the replacement is rolled per card rather than being one Discovered card.

### cult_recruiter  [needsEngine -> src/engine/effects/ops/shop.ts]
Low priority, catalog-wide. expiryTurn (shop.ts:24-32) maps `duration:'turn'` to `s.turn`, and lockIsActive returns `state.turn < lock.expiresOnTurn` — so every `duration:'turn'` lock in the catalog is inert the moment it is applied. shop/locks.ts expiryTurnFor maps the same duration to `turn + 1`. I worked around it on this card with `{turns: 1}`, but the two functions should agree.

### profe_yates_unleashed  [needsEngine -> src/engine/types.ts + src/engine/effects/select.ts]
CardFilter has no substring/regex facility, so `filter:{name:'/[moseMOSE]/'}` matches nothing and the (5) Legendary is a blank. Add `nameContainsAny?: string[]` to CardFilter, matched case-insensitively against def.name in BOTH matchesFilter and matchesDefFilter, then author `filter:{ nameContainsAny:['m','o','s','e'] }`. Card left alone: enumerating ~500 defIds is not a fix, and any approximation (trash the whole hand) is far stronger than a (5).

### hired_shrimp  [needsEngine -> src/engine/types.ts + src/engine/effects/select.ts]
`filter:{name:'wordsFewerThan:16'}` is a magic string compared verbatim to def.name, so nothing is trashed. Needs a real predicate, e.g. `wordCountLt?: number` on CardFilter resolved against the target definition's `wordCount`, falling back to counting whitespace-separated tokens of def.text; then author `filter:{ wordCountLt:16 }`. The card's own `wordCount` is now consistent (see fixed).

### counter_spell  [needsEngine -> src/engine/types.ts (NextCardMod) + src/engine/core/play.ts]
The counter is spelled as `buyTo` on a play-scoped mod and the play path never reads buyTo. There is no cancel channel at all: consumePlayMods handles only multiply/multiplyStats/grantKeyword/appendEffects/buffTimes/nerfTimes/absorbInto/bind, and appendEffects run AFTER the stat line and printed effects, so nothing authorable can stop a card resolving. Add `cancelTo?: Zone` plus `onlyTypes?: CardType[]` to NextCardMod, honoured in playCard before step 4, and author `{who:'eachOpponent', appliesTo:'play', cancelTo:'gy', onlyTypes:['Action'], uses:1}`. Card left alone (its current mod is inert, not harmful).

### aggressive_taxation  [needsEngine -> src/engine/core/play.ts + src/engine/effects/ops/timing.ts]
Money cannot be redirected to the mod's author: appendEffects always resolve in the player's own frame, so the delayed `gain` pays the OPPONENT the cost of the card they played, and there is no Who meaning 'the controller of the source card'. There is also no way to gate a NextCardMod to Resources or to read the played card's money output. Needs a Who (or delayed payee) resolved from the mod's/entry's sourceIid owner, plus `onlyTypes` on NextCardMod. Card left alone deliberately: every card-only rewrite either invents a different card (pure denial) or pays third parties in 3+ player games.

### clipped_wings  [needsEngine -> src/engine/core/buy.ts]
The Cursed Pig rider hangs on `appendEffects` of an `appliesTo:'buy'` mod, and the buy path reads only costDelta/costFloor/buyTo — appendEffects is unreachable there, and consumePlayMods skips the mod. Either run a buy mod's appendEffects in buyCard with the buyer as player and the bought instance as sourceIid (so `selfCost` reads the bought card), or give NextCardMod a real expiry ('untilYourNextTurn') and fire the rider from an onOpponentBuy trigger that can see the bought card. `uses:99` also needs replacing with that expiry. Card left alone (currently inert, +1 Action only).

### the_curator  [needsEngine -> src/engine/effects/ops/choices.ts]
`{op:'choose', who:'eachOpponent'}` calls pickChooser = rng.pick(opps) and raises ONE prompt, so a (5) Epic taxes one random opponent no matter how many are at the table. opChoose needs to enqueue one prompt per resolved opponent.

### the_curator  [needsEngine -> src/engine/core/resume.ts + src/engine/effects/runtime.ts]
Both branches run in the CHOOSER's frame (localResume uses makeContext(prompt.player)), so `who:'self'` is the opponent and the hand branch's moveTo lands in their own GY — nothing can reach the Curator's controller. Needs a Who meaning 'the controller of the source card' (the prompt already carries ctx.sourceIid). Card left alone: with `pick:'choose'` both branches are currently inert rather than harmful, and the only card-only rewrite ('chosenOpponent' from the chooser's frame) is correct in a duel and wrong with 3+ players.

### the_curator  [needsEngine -> src/engine/types.ts (NumericFilter) + src/engine/effects/context.ts]
The 'costing at least your cheapest revealed card' floor has no representation: nothing records a cost at reveal time, there is no `cheapestInHand` expression variable, and NumericFilter takes plain numbers so a filter cannot be `{cost:{gte:{expr:...}}}`. Needs both (a var in buildVars + EXPR_VARS, and Amount accepted in NumericFilter).

### siphon_squad  [needsEngine -> src/engine/effects/select.ts + src/engine/effects/opkit.ts]
The hand-size ladder is evaluated over every opponent's hands merged (selectInstancesWith concatenates them) and each discard's `count` is a table-wide total, so three opponents holding 2 cards each all count as '4+' and 2 cards total are discarded. Needs a per-opponent effect frame (a forEach over players, or per-player `count` semantics on a multi-player selector). Card left alone: the ladder shape is right, and the only per-player mover available from card data is `recruit`, which moves cards without firing onDiscard — wrong for a discard attack.

### meta_shift  [needsEngine -> src/engine/effects/ops/movement.ts + src/engine/types.ts (Who)]
'Each player passes a card to the next player' is unauthorable: moveTo recomputes the owner from `i.owner` so a hand→hand move is a no-op, `count` on a `who:'eachPlayer'` selector is one card in total, and there is no Who meaning 'the next seat in state.playerOrder'. Needs a destination-player on moveTo (or a `nextPlayer` Who) plus per-player counts. Card left alone (currently inert).

### pickle  [needsEngine -> src/engine/effects/ops/timing.ts]
The Gold option runs inside a delayed entry framed on the opponent, where `who:'chosenOpponent'` resolves to THEIR highest-VP opponent — a third player with 3+ at the table. opDelayed already stores sourceIid on the entry; resolving a Who against the source card's owner (or adding such a Who) would pay the Pickle player. Correct today only in a duel; card left alone because 'chosenOpponent' is already the closest available approximation.

### doomsday_clock  [needsEngine -> src/engine/effects/ops/timing.ts or src/engine/effects/ops/misc.ts]
'Any player replaying this resets the timer' is not implemented: opDelayed only ever pushes a new entry, and no op can clear or reschedule an existing one. Either give the timer a single rewritable home (`state.hardEndTurn = state.turn + 3 * playerCount`, which localEndCondition already ends on and SB-41 gives no lap of honour), or have `delayed` replace existing entries carrying the same reason across every player's list.

### ebon_hand  [needsEngine -> src/engine/effects/triggers.ts (discardWithTrigger)]
Residual after the card-only fix: 'the last card an opponent discarded' has no engine representation — nothing records a discard batch or sequence, so the selector can only reach 'most expensive in that graveyard'. Needs a per-player discard-sequence counter (or a lastDiscarded marker) written by discardWithTrigger and a filter that reads it.

### war  [needsEngine -> src/engine/effects/ops/movement.ts + src/engine/effects/select.ts]
Residual after the card-only fix: when an opponent's bet is dearest the pot cannot be handed to them (no Who can name the owner of a selected card, and moveTo cannot change owners), so every bet returns to its owner's GY instead; and a tie cannot be detected (no comparison of two revealed cards' costs), so the printed 'ties re-bet 3 more cards' clause is dropped and equal costs fall to seat order. A real `steal`/owner-changing move plus a cost-of-selected-card variable would let the printed row be authored in full.

### mother_witch  [needsEngine -> src/engine/effects/ops/choices.ts + src/engine/effects/pools.ts]
Residual after the card-only fix: a Discover prompt always goes to `item.player` and a pool's zone scopes ignore `who` (zoneDefIds collects ctx.player's zones), so 'each opponent Discovers' cannot be authored — the opponent no longer chooses which card becomes the Pig. Needs per-player effect frames (or an explicit prompt player on discover) plus `who` honoured for zone scopes in zoneDefIds; the same `who` gap affects Spyglass, Griftah and Bribe pools with 3+ players.

### the_curator  [needsEngine -> src/engine/core/resume.ts]
Catalog-wide, found while fixing these cards and worth flagging beyond my files: an op that suspends through resolveTargets/resolvePiles (`pick:'choose'`) is LOST on resume. The prompt is built with mode 'targets'/'piles' and `then: []`, and localResume has no branch for it — it runs no optionEffects, no `then`, and only the 'discover' type has a default — so the suspended node never resolves. Every `pick:'choose'` target selector in the catalog silently does nothing whenever there is a real choice to make. Fixing it means routing those prompts back through the op with `pre.iids`/`pre.pileIds` (export a `resumePrompt` from @engine/effects, which core/resume.ts already probes for). I avoided `pick:'choose'` in my rewrites and used `selectCards` (which does resume) instead.

### tanyays_unstable_element  [needsEngine -> src/engine/core/turn.ts (plus core/play.ts consumePlayMods and effects/ops/replay.ts multiplierFor)]
The 'no more draws this turn' rider needs a per-turn no-draw flag that drawCards/opDraw/applyStats honour and resetTurnStats clears. I removed the multiply:0 nextCardModifier rather than leave it doing damage — on the normal play path consumePlayMods skipped it while still spending a use, and on the effect-driven path multiplierFor ignored multiplyStats and zeroed the entire stat line of the next 99 plays, never cleared at turn end. The card is now just 'Flimsy. Draw your entire Library.' and the rider is off the text until the flag exists.

### sleepy_joe_bider  [needsEngine -> src/engine/core/turn.ts (plus effects/ops/timing.ts opEndTurn)]
Left the card alone. counters.endTurnRequested is written and read nowhere, so the turn never actually ends, and there is no skip-discard flag for endTurn step 2 to honour. The only card-only route to 'without discarding' is parking the hand in `aside` and pulling it back with a startOfNextTurn delayed moveTo, which would drag any Hand Box storage out with it across a turn boundary — not worth it. Needs the counter consumed in the action loop plus a skip-discard flag.

### sketch_artist  [needsEngine -> src/engine/types.ts CardFilter (and src/engine/effects/select.ts)]
(missing-clause) 'an Action that grants no +Actions' cannot be filtered: CardFilter has no predicate over a card's stat line, so even a named filter cannot express it. Wants either a stats predicate on CardFilter or a NAMED_FILTERS entry backed by one. Until then the clause is off the card text and any Action qualifies.

### counting_cards  [needsEngine -> src/engine/effects/context.ts / evaluate.ts]
The ace rule — a (1)-cost card counting as 11 or 1, whichever keeps the total <= 21 — needs a per-card conditional cost the expression layer does not have. Everything else in SB-21 is implemented; the clause is off the text.

### star_compass  [needsEngine -> src/engine/effects/ops/control.ts]
The doc's 'repeat' needs a loop that can stop on a condition ({op:'repeat'} always runs its full count and there is no break). The same-name check itself is implemented; only the re-check of each new pair is missing.

### shadiris_visions  [needsEngine -> tools/validate-cards.ts (and test/catalog-integrity.test.ts)]
CROSS-CUTTING, not mine to edit: the validator's B97 check rejects the sentinel — 'createCard references unknown defId "$discovered"' for shadiris_visions and 'transform references unknown defId' for model_citizen — and test/catalog-integrity.test.ts fails the same way. A dozen other agents' cards (welfare, cncias, spyglass, mewing, homebrew, the scarabs, ...) trip it too. Both places need to skip DISCOVERED_SENTINEL / SELECTED_SENTINEL before the registry lookup.

### ricochet  [needsEngine -> src/engine/effects/ops/misc.ts, src/engine/core/turn.ts]
'One Ricochet per turn' cannot be written card-side. The restriction is subtype-wide and player-scoped (all three Ricochets share one use), but addCounter only writes instance counters and there is no player-scoped counter op, no reader for PlayerState.counters.ricochetUsedThisTurn, and nothing clears it in resetTurnStats. A per-instance approximation is also unavailable: buildVars only surfaces a counter named counter/uses/charges, and before that key exists selfCounter falls back to the sum of all counters — which already includes the playCount playCard writes before the effect body runs — so an {expr:'selfCounter == 0'} gate is false on the very first use. Needs a player scope on addCounter (or a dedicated per-turn flag) plus a clear in resetTurnStats. Card left alone.

### ricochet_plus  [needsEngine -> src/engine/effects/ops/misc.ts, src/engine/core/turn.ts]
Identical to Ricochet: the once-per-turn gate needs a player-scoped counter cleared in resetTurnStats, shared across the Ricochet subtype. Card left alone.

### ricochet_plus_plus  [needsEngine -> src/engine/effects/ops/misc.ts, src/engine/core/turn.ts]
Identical to Ricochet: the once-per-turn gate needs a player-scoped counter cleared in resetTurnStats, shared across the Ricochet subtype. Card left alone.

### wombo_combo  [needsEngine -> src/engine/systems/combo.ts, src/engine/effects/ops/]
'Permanently gain the Combo clause of a random Combo card' needs an effect op wired to stealComboClause(state, thief, victim), which copies the victim's clauses onto the thief's inst.extraEffects (playCard composes its body as [...def.effects, ...inst.extraEffects, ...], so an absorbed clause would replay forever). The primitive ships and is exported but no card, op or system calls it; {op:'nextCardModifier', mod:{absorbInto}} is not a substitute because opNextCardModifier only fills absorbInto when mod.bind is set. CARD-ONLY IMPROVEMENT APPLIED so the card is not left half-fixed: the tally counter was renamed from 'absorbedClauses' to 'counter' (the key buildVars exposes to expressions — 'absorbedClauses' made selfCounter read the sum of every counter, playCount included), and the five imitation clauses are now wrapped in {op:'repeat', times:{expr:'selfCounter'}}, so the Nth Combo-1 play resolves N random clauses instead of always exactly one. Text updated to match; verified the counter reads 1 then 2 across two plays.

### synchro_summon  [needsEngine -> src/engine/types.ts, src/engine/effects/select.ts]
'2 same-cost cards' and 'an Action of that cost' are not expressible. CardFilter.cost is a NumericFilter of literal numbers only (src/engine/types.ts:257) with no dynamic or bound form, and nothing binds a selected/discarded card's cost into ctx.vars. Needs either a dynamic NumericFilter (e.g. {eq:{expr:...}}) or a cost binding written by selectCards/discard. Structural half fixed; text reprinted so it no longer promises the linkage.

### ritual_summon  [needsEngine -> src/engine/types.ts, src/engine/effects/select.ts]
'An Action of that cost' — same missing cost binding as Synchro Summon; the trashed card's cost is never carried into scope. Sequencing half fixed; text reprinted.

### fusion_summon  [needsEngine -> src/engine/types.ts, src/engine/effects/select.ts]
'An Action costing their sum' needs both a dynamic NumericFilter and an op that accumulates the cost of what was discarded; neither exists. Structural half fixed; text reprinted. BALANCE CONSEQUENCE worth flagging: with the cost band gone, Fusion Summon (4) is now word-for-word Synchro Summon (1), so it is strictly worse until the linkage lands.

### golden_scarab  [needsEngine -> src/engine/core/buy.ts, src/engine/core/play.ts]
'When played, Gold is added to the options' needs a signal that a resolution came from Play on Buy. buy.ts moves the instance to hand and calls playCard with the same def.effects, and playCard's opts are not threaded into ctx.vars, so the two are indistinguishable. selfPlayCount is not a discriminator: playCard increments p.playCounts by defId, so a second bought copy already reads 2 on its own Play-on-Buy resolution. Needs a var set on buy.ts's playCard call (e.g. vars.fromBuy). Text reprinted meanwhile.

### empyreal_scarab  [needsEngine -> src/engine/core/buy.ts, src/engine/core/play.ts]
'(5)-cost to GY; when played, (6)-cost instead' needs the same 'played from buy' signal as Golden Scarab. Text reprinted to the single (5)-or-(6) pool meanwhile.

### too_many_stats  [needsEngine -> src/engine/types.ts, src/engine/effects/context.ts, src/engine/effects/ops/misc.ts]
Two engine gaps the card now works around rather than needing. (1) Condition.combo takes a literal number with no expression form, which is why the Combo roll had to be packed into the `counter` key and read as {expr:'comboCount >= floor(selfCounter / 10)'}; a {combo:{expr:...}} form (or a second named counter exposed to buildVars) would let the two rolls live in separate, self-describing counters. (2) There are no set-semantics primitives — no setCounter op, and the buff/nerf node has no duration field — which is why every roll has to book a delayed end-of-turn inverse. Both work today; noting them so the workarounds can be retired.

### jeweled_scarab  [needsEngine -> tools/validate-cards.ts, test/catalog-integrity.test.ts]
NOT a card defect — a validator/test gap that my three Scarabs now trip along with 12 other cards from other agents (cncias, homebrew, mathemagiks, mewing, spyglass, yuyas_embrace, welfare, mediator, shadiris_visions, glubby_gloob_the_auctioneer, kwzki_high_council_consultant, and model_citizen via transform). tools/validate-cards.ts checks `typeof ref === 'string' && !known[ref]` on createCard/transform defIds and rejects the DISCOVERED_SENTINEL; test/catalog-integrity.test.ts's B97 tests do the same. Both need to whitelist '$discovered' and '$selected'. I was told not to edit tools/ or test/, so this is unfixed and both were already failing on the baseline before I touched anything.

### chains_of_the_sovereign  [needsEngine -> src/engine/core/resume.ts]
Pile prompts are dropped, which is why three cards in this file use a deterministic pick instead of `pick:'choose', count:1`. On suspend, runQueue has already `q.shift()`-ed the node and re-parks only the REST of the queue, and localResume handles just `optionEffects` and `type:'discover'` — a 'selectPile' prompt with `then: []` resolves to nothing, even though its ctx already carries `{ node, mode:'piles' }`. Re-dispatch `prompt.ctx.node` with `pre.pileIds` on resume, then Chains / Seal the Rift / Scripture of Kwzki can go back to `pick:'choose', count:1` and prompt for real.

### chains_of_the_sovereign  [needsEngine -> src/engine/core/turn.ts]
Dead clause, half-fixed. I added `zones: ['play','hand','gy','trash']` to the onUnlock trigger so it survives Chains' own Flimsy trash (the default trigger zones are play/hand/field). The printed case still cannot fire: expireShopTimers filters `pile.locks` and logs 'unlock' without ever calling fireEvent(...,'onUnlock',...), so a natural expiry charges nobody — only an explicit {op:'unlockPile'} fires the event. Fire 'onUnlock' from the sweep; and the trigger is still unscoped (it pays out on any unlock of any pile by anyone), which needs a lock->source binding no card selector can express.

### chains_of_the_sovereign  [needsEngine -> src/engine/effects/context.ts]
Wrong condition, left alone — there is no card-side lever. `lockedPiles` comes from emptyAndLocked, which walks EVERY pile in state.shop.piles (4 resource + 3 points + 22 prophet + 10 draft) and counts `pile.locks.length > 0` without consulting lockIsActive, so inert locks (Idol wrote 23 of them until the next sweep) and other shops' locks all feed the gate, while 4 of 10 locked draft piles do not trip it. The literal 5 in `lockedPiles < 5` is also only half of config.draftPileCount at its default. Count live draft locks (isLocked/lockIsActive) and expose the configured draft pile count, then the gate can be written against it.

### ebon_blade  [needsEngine -> src/engine/effects/select.ts]
Wrong target, card left unchanged — no card-side idiom exists. selectInstancesWith concatenates every resolved player's zones into ONE candidate list and applies `count`/`pick` to the merged result, so `{who:'eachOpponent', count:1, pick:'mostExpensive'}` trashes one card at the table, not one per opponent. opForEach binds instances, not players, so it cannot loop the opponents card-side, and `count: playerCount-1` would take the N priciest overall (possibly all from one player), which is not what the row prints. Apply count/pick per resolved player when `who` yields more than one. Systemic: ~15 other cards print 'each opponent ... N' with the same shape.

### scripture_of_kwzki  [needsEngine -> src/engine/core/buy.ts]
Dead clause, node left in place as the correct authoring shape. peekBuyMods/consumeBuyMods read only costDelta, costFloor and buyTo from an `appliesTo:'buy'` mod and ignore grantKeyword, while consumePlayMods — which does honour grantKeyword — skips buy-scoped mods, so no bought card ever gains PlayOnBuy. Carry grantKeyword onto the bought instance's addedKeywords in consumeBuyMods, before buyCard's PlayOnBuy check. Related: the mod is tied to no pile and nothing clears nextCardMods at end of turn, so its remaining `uses: 9` leak into later turns — a pile-scoped, turn-lifetime mod would fix both.

### kwzki_high_council_consultant  [needsEngine -> tools/validate-cards.ts]
New, blocking, and shared with other agents: `npm run cards:validate` and test/catalog-integrity.test.ts both reject `{op:'createCard', defId:'$discovered'}` as an unknown defId — the very idiom the engine-fix note tells us to use. 12 cards now trip it (kwzki_high_council_consultant, mediator, welfare, glubby_gloob_the_auctioneer, yuyas_embrace, mewing, cncias, jeweled_scarab, golden_scarab, empyreal_scarab, mathemagiks, homebrew). Exempt DISCOVERED_SENTINEL / SELECTED_SENTINEL ('$discovered' / '$selected') from the createCard/addToPileTop/transform defId check in tools/validate-cards.ts line ~217 and in the B97 test at test/catalog-integrity.test.ts:158-166.

### safety_net  [needsEngine -> src/engine/effects/triggers.ts]
Confirmed dead and left alone. 'onTrash' is in SELF_EVENTS, so fireEvent only ever offers it to the trashed instance, and trashWithTrigger calls trashInstance before fireEvent, so the subject's zone is already 'trash' and `zones:['play']` skips it even then. A trash redirect is not expressible in card data: trashWithTrigger needs to consult a per-player redirect flag before moving the card, divert it to 'gy' and draw. I did not apply the finding's "at minimum drop zones" suggestion — since onTrash is self-only, the trigger could then only ever rescue Safety Net itself out of the global trash, which is not the printed card and would read as a working card that does the wrong thing. Its condition and target additionally need rewriting once the hook exists, because a selector for zone 'trash' ignores `who` (select.ts:253-255).

### the_fall_guy  [needsEngine -> src/engine/effects/triggers.ts]
Same dead trigger, left alone. A bystander sitting in the library can never see another card's trash: SELF_EVENTS pushes only the subject, and the zone check runs after the move. It needs trashWithTrigger to offer a table-wide, pre-move event carrying the subject iid so a listener can substitute itself, plus the trash source in the trigger vars for the printed 'non-Flimsy effect' guard. No card-only approximation exists; the +1 Card stat line is all it does today.

### cookie_gruzzler  [needsEngine -> src/engine/effects/ops/movement.ts]
Card-only improvement applied, but the printed "to your GY" still needs opMoveTo to take an owner override: a moved instance keeps `i.owner`, so an opponent's card can only ever be re-homed to the opponent's GY. What I did instead: the eaten cards go to 'aside' (keeping the exact instances, instead of being unfindable in the global trash, which a selector reads table-wide and which handed back this card itself); the counter is re-keyed 'counter' because buildVars only honours counter/uses/charges and otherwise summed in playCount; and on trash it `copyCard`s the aside cards into your GY (copies, since a move cannot re-home them) and then trashes the originals. Second engine gap, in src/engine/effects/select.ts: a Selector's `count` is a global total, not a per-player quota, so `{who:'eachPlayer', count:1, pick:'top'}` ate exactly one card — always the caster's own. I worked around it with two nodes (self, then eachOpponent), which is exact at 2 players and still under-eats at 3+. Probed at 2 players: both library tops go aside, and trashing the Gruzzler puts both into your GY and none into the opponent's.

### constellation  [needsEngine -> src/engine/core/scoring.ts]
The winner-deciding score (computeScores -> scoreFor) never runs the run-walking implementation that already exists. src/engine/meta/scoring.ts:133 scoreConstellation walks 1..X over untrashed deck cards, marks exactly those trashed, and returns X, and scoreFor there resolves Constellations FIRST because they consume cards the other End of Game scorers read — but that path is only reached via meta liveVp (win conditions / sim heuristics), not from core/endgame.finishGame. Route core/scoring.computeScores (and engine index finalScores) through the meta implementation, or port scoreConstellation + the EndOfGame handling into core/scoring.ts. With my card edit the definition is now inert by design, so there is no double-count risk from either route.

### oh_mr_lebon  [needsEngine -> src/engine/types.ts (EXPR_VARS) + src/engine/effects/context.ts (buildVars)]
Add 'tallestOpponentLibrary' — max of state.players[oid].library.length over opponentsOf(state, player), 0 with no opponents. The card now reads `libraryHeight > tallestOpponentLibrary`; until the variable exists, expr.ts readVar throws 'unknown identifier' and evalCondition catches it as false, so the card scores nothing (safe, but a silent zero — nothing validates var names, only count() filter names). Note pvp.ts:260 carries the identical dead pattern and belongs to another agent.

### the_conglomerate  [needsEngine -> src/engine/types.ts (EXPR_VARS) + src/engine/effects/context.ts (buildVars)]
Add 'vpInHand' — the SUM over the player's hand of statOf(state, iid, 'vp') plus counters.vp (and instance.secret.vp, matching core/scoring.vpOnInstance). countIn can only count cards, so 'their total VP' has no expressible form today. The card now reads {expr:'vpInHand'}; until the variable exists it evaluates to 0 via evalAmount's catch, which is the same value the dead countIn produced, so this is not a regression. Skyscraper ships with stats {} on purpose — its whole value is the vp counter this op writes.

### monumental_works  [needsEngine -> src/engine/types.ts (EXPR_VARS) + src/engine/effects/context.ts (buildVars)]
Add 'maxRelicUpgrades' — the maximum of instance.counters['upgrades'] over Relics in the player's deck (library + hand + gy + play), 0 if none. That is the only reading that satisfies both the doc row and the printed 'most upgraded Relic ... no double counting'. Alternative if you prefer a selector: a 'mostUpgraded' pick verb on Selector/selectInstancesWith ranking by counters.upgrades, paired with a body that reads that counter specifically (NOT selfCounter, which sums all counter keys). The card currently reads {expr:'maxRelicUpgrades'} and evaluates to 0 until one of these lands.

### bullseye  [needsEngine -> src/engine/effects/select.ts (selectInstancesWith) + src/engine/types.ts (Selector)]
'One card in EACH opponent's hand' is not expressible: selectInstancesWith pushes every resolved player's zone into one candidates array and then slices `count` off the front of the whole pool. Add a `perPlayer?: boolean` to Selector and apply count per resolved player before merging, then set bullseye's nerf target back to `{who:'eachOpponent', zone:'hand', count:1, pick:'random', perPlayer:true}`. My `playerCount - 1` stand-in also miscounts when a player is eliminated (opponentsOf filters via livePlayers, playerCount does not), though selectInstancesWith clamps to the pool so it only under-delivers.

### chopped_chuzz  [needsEngine -> src/engine/systems/fuse.ts]
Confirmed and left alone — the card definition genuinely cannot express it. 'When this attempts to Fuse, trash it instead' has no card-level hook: TriggerEvent has no fusion event and EffectNode has no fuse op, and fusedDefinition() is pure with no special case (only a descriptive comment at fuse.ts:22 mentions chopped_chuzz). The exclusion belongs in fuseForCard/fusedDefinition: refuse chopped_chuzz (or any anti-fuse flagged defId) and trash that instance instead. Still unobservable today because src/cards/advanced/discovery.ts:31 defines fuseForCard and nothing calls it. Card unchanged, text left as printed.

### yuyas_embrace  [needsEngine -> tools/validate-cards.ts (line 217, 221, 257) + test/catalog-integrity.test.ts (lines 163, 178)]
SHARED, BLOCKS THE GREEN BUILD — not specific to my file. The defId-resolves check does not exempt the discover/select sentinels, so every correct use of '$discovered' now fails both the validator and catalog-integrity. 14 cards across many agents' files are flagged: kwzki_high_council_consultant, shadiris_visions, mediator, welfare, glubby_gloob_the_auctioneer, yuyas_embrace, spyglass, mewing, cncias, jeweled_scarab, golden_scarab, empyreal_scarab, mathemagiks, homebrew, plus model_citizen via the transform check. Fix: skip DISCOVERED_SENTINEL ('$discovered') and SELECTED_SENTINEL ('$selected') in the createCard/addToPileTop/transform defId checks in both files. I cannot edit tools/ or test/.

### iced_up  [needsEngine -> src/engine/core/resume.ts (localResume, lines 112-114)]
Not blocking my card (I routed around it) but it is a live trap for every `min: 0` selectCards in the catalog. When a selection prompt resolves with NO cards picked, `perChoice.length === 0` falls into `runEffects(s, prompt.then, ctx)` — the continuation runs ONCE with ctx.sourceIid still bound to the card that asked. For a `then` of [discard self, +2 Money] that discards the source card from play and pays out for declining. legalResolutions offers `[]` whenever prompt.min === 0, so a simulator will find it. Suggested fix: when prompt.type is 'selectCards' (or whenever the prompt carried perChoice) and nothing was chosen, run `then` zero times.

### shining_kit  [needsEngine -> src/engine/core/play.ts (plus src/engine/effects/ops/buff.ts and the NextCardMod type in src/engine/types.ts)]
Card left alone — no card-only fix exists. Confirmed all three breaks: runBuffNode's nextPlayed branch (buff.ts:77-88) pushes {buffTimes, appliesTo:'play', uses:1} and drops `stat`; NextCardMod has no card filter, so nothing can restrict it to a Resource; and buffTimes is write-only — consumePlayMods accumulates it at play.ts:167-168 and playCard never reads it (grep for buffTimes|nerfTimes across src/ returns only those writes and the type declarations at types.ts:394-395). Needs: buffStat?: StatKey (and an optional filter) on NextCardMod, set in buff.ts's nextPlayed branch, consumed in playCard after the stats step as `for (let k=0;k<mods.buffTimes;k++) applyBuff(s,'instance',iid,+1,mods.buffStat ?? null,rng)` with the nerfTimes mirror. The same gap silently disables src/cards/tribes/cn.ts:28 and src/cards/tribes/food.ts:805 and :826, which are other agents' files.

### blood_diamond_cutter  [needsEngine -> src/engine/shop/ (a buy-time cost hook) — the pile price is read via priceFor at src/engine/core/buy.ts:154]
Partly fixed card-side; the printed cost REDUCTION still needs an engine hook. Confirmed the startOfTurn/zones:['shop'] trigger is unreachable — fireOwnedTriggers (src/engine/core/triggers.ts:108) builds candidates from [...p.play, ...p.hand, ...p.gy, ...p.library] and a shop instance has owner null, so it sits in none of them; and even a working discount would land too late because onBuy fires at buy.ts:183, after `p.money -= price` at buy.ts:159. Applied card-only: deleted the dead startOfTurn trigger and replaced the unconditional, uncapped, choiceless onBuy trash with a selectCards (min 0, max 99) over hand Actions costing (1) or less whose `then` trashes each pick and gains 2 Money — a refund rather than a discount, so the card is no longer pure downside. Text rewritten to match: 'When you buy this you may trash any number of Actions costing (1) or less from your hand; each one refunds (2) Money. +1 Action, +5 Money.' A real discount needs a shop-side hook that lets a pile's own card modify its price at purchase time; if that lands, revert to modifyCost scope:'pile' and restore the original text. Precedent for a prompting op inside an onBuy trigger already exists at src/cards/tribes/plague.ts:77.

### advanced_refining  [needsEngine -> src/engine/effects/ops/movement.ts (opTransform, lines 196-201; pickByCostDelta at 219-234)]
My two cards are already fixed card-side with literal defIds, so this entry is for the OTHER files that still use the broken form. opTransform never calls upgradedDefId()/downgradedDefId() from src/engine/systems/upgrade.ts (grep shows only systems/index.ts re-exports them) — it routes into:'upgrade'/'downgrade' to pickByCostDelta, a random pick over every purchasable card at oldCost±1 from the entire universe. Root fix: in opTransform, resolve 'upgrade'/'downgrade' through upgradedDefId()/downgradedDefId() when the source defId is on RESOURCE_LADDER, falling back to pickByCostDelta otherwise; opTransform's existing `if (!nextDefId || nextDefId === oldDefId) continue;` then gives 'Diamond stays a Diamond' and 'Copper stays a Copper' for free. Remaining broken call sites, both other agents' files: src/cards/advanced/chaos.ts:189 and src/cards/advanced/recursion.ts:433. My card-only rewrites use literal defIds, so they stay correct whether or not this lands.

### lead  [needsEngine -> src/engine/core/turn.ts]
Confirmed dead. The cost reroll is a `{on:'startOfTurn', zones:['shop']}` trigger, but startOfTurn is dispatched only by fireOwnedTriggers at turn.ts:164, whose candidate list is literally `[...p.play, ...p.hand, ...p.gy, ...p.library]` (triggers.ts:108) — shop piles are never scanned, and a Lead in hand/gy fails the zones gate at triggers.ts:75 anyway. I re-grepped every dispatch site (buy.ts:184/185/188/201, play.ts:202/284/287, turn.ts:164/204/217/219, endgame.ts:144); none passes a shop instance. Needs a start-of-turn shop sweep in startTurn (after turn.ts:164): walk state.shop.piles, take topOfPile, and fireInstanceTriggers(s,'startOfTurn',player,iid) for instances whose def declares a zones:['shop'] trigger. Card left untouched — no card-only route exists. Second, smaller problem for the same fix: even with a working reroll the payout `{expr:'selfCost'}` resolves against the instance in 'play', and instanceCost returns defCost for any non-shop instance (runtime.ts:143-151), so it pays a flat 4 rather than the price paid; that needs the paid price bound into the buy-time trigger vars (same mechanism as rebate). The SB-27 self-lock is fine — that onBuy trigger declares no zones and buy.ts:184 fires it on the bought instance.

### pure_of_heart  [needsEngine -> src/engine/shop/cost.ts]
Confirmed dead twice over. The (0) price is a `{on:'startOfTurn', zones:['shop']}` trigger that no dispatcher reaches (same route as lead), and its condition ('hand is empty') is sampled at the one moment the hand is guaranteed to hold 5 cards — endTurn draws the next hand at turn.ts:224-226 before passing the turn, and start-of-turn card triggers fire at turn.ts:164. A shop sweep alone would NOT repair this card. The price has to be a purchase-time read: costOf (shop/cost.ts:91-109) or priceFor (buy.ts:67-81) needs to see `p.hand.length === 0` for this def. There is no dynamic-cost hook on CardDefinition (types.ts:110-141) and no selector/condition that can compare against a live hand at buy time, so no card-only fix exists. Card left untouched; its text already promises the live reading.

### giants_aid  [needsEngine -> src/engine/shop/cost.ts]
Confirmed dead. The discount hangs off `{on:'onPlay', zones:['shop']}`, but onPlay is dispatched only to the instance being played (play.ts:284); a card sitting in a shop pile is never played, and opponents only get onOpponentPlay via the owned-instance sweep. Even one firing would subtract 1, not 1 per card played. Needs a cost-time read: compute `base - p.playedThisTurn.length`, floored at 0, in costOf/priceFor for this def. A trigger-based repair would need both the shop sweep AND a per-card-played event. Card left untouched — no card-only expression can reach a pile's price.

### rebate  [needsEngine -> src/engine/core/buy.ts]
Finding 1 of 2 (dead-trigger). Confirmed: buy.ts:184 is the only onBuy dispatch and it is fireInstanceTriggers on the bought card alone; there is no owned-instance sweep for onBuy anywhere in src/. Doubly dead — the bought instance was already moved to `mods.buyTo ?? 'gy'` at buy.ts:167, so even a Rebate bought this turn fails the zones:['play'] gate at triggers.ts:75. Needs a buyer-side sweep after buy.ts:184 restricted to triggers that declare a `zones` list; a bare fireOwnedTriggers(s,'onBuy',...) is NOT safe, because lead's zone-less onBuy lockPile trigger would then lock the Lead pile from the buyer's gy or library on every purchase. Card left untouched.

### rebate  [needsEngine -> src/engine/core/buy.ts]
Finding 2 of 2 (wrong-expression), must land with the dispatch fix. The refund is `{expr:'ceil(selfCost * 0.6)'}`, but a trigger's context source is the instance carrying the trigger (triggers.ts:82) and buildVars sets selfCost = instanceCost(state, sourceIid) (context.ts:154), which returns defCost for a non-shop instance (runtime.ts:143-151) — so Rebate in play refunds a constant ceil(3*0.6) = 2 for a (10) buy and a (0) buy alike. No var in the frozen EXPR_VARS list (types.ts:196-227) carries the price just paid. Fix: bind the price at the dispatch site — buy.ts already has `paid` at line 158 — e.g. makeContext(player, iid, 0, 1, { x: paid }), then author the refund as `{expr:'ceil(x * 0.6)'}` (x is in EXPR_VARS and `extra` overrides computed values, context.ts:158/177-182).

### professor_of_curvature  [needsEngine -> src/engine/effects/ops/shop.ts]
Confirmed: opModifyCost's nextBuy branch (shop.ts:107-121) never calls make() — it pushes a raw NextCardMod `{costDelta: delta ?? 0, costFloor: floor, appliesTo:'buy', uses:1}` and discards setTo entirely. With setTo:0 and no delta that is costDelta 0, costFloor 0, so priceFor (buy.ts:67-81) computes base + 0 and then clamps UP to 0 — no discount at all, and it actively cancels the credit from a negative-cost buy (a Series A Funding at (-1) becomes (0), turning a +1 Money payout into nothing). Nothing gates the mod to a pile priced at currentTurn either. Needs two engine changes: (1) carry `setTo` and an optional cost filter onto the NextCardMod in shop.ts, and teach peekBuyMods/priceFor (buy.ts:34-81) to honour setTo and skip the mod when the pile's base price does not match; (2) then the card can be authored as a nextBuy mod gated to a pile priced == currentTurn. I deliberately did NOT swap setTo:0 for delta:{expr:'0 - currentTurn'} — with no gate that discounts every card, not the (X)-cost card the doc names, and the floor would still zero out negative-cost buys. Card left untouched.

### the_biggest_the_largest  [needsEngine -> src/engine/effects/context.ts]
Confirmed and HIGH EXPOSURE — please prioritise. `{expr:'max(0, deckSize - madOfOpponentDeck)'}` subtracts a cost dispersion, not a deck size: vars.madOfOpponentDeck is the mean absolute deviation of every opponent card's instanceCost (context.ts:93-96, 116, and meanAbsoluteDeviation at 40-46). On turn 1 against a mirror starting deck (7 Copper at (0), 3 Tix at (2)) MAD is 0.84, so this (1)-cost card pays ~9 Money, and the payout tracks your own deck size all game whether or not you lead; with no opponents it pays the full deckSize. There is also no largest-deck gate at all. Needs a new var: `vars.largestOpponentDeck = max over opponents of deckOf(state, oid).length` in buildVars, appended to the frozen EXPR_VARS list in src/engine/types.ts:196-227; the card then becomes `{expr:'max(0, deckSize - largestOpponentDeck)'}`, which is simultaneously the gate and the size lead. I checked every card-only route: no opponent deck-size var exists, `count(x)`/`countIn(zone,x)` only see your own deck plus the global shop/trash zones, and Condition.has.atLeast is a plain number (evaluate.ts:150) so it cannot be compared against deckSize. Card left untouched as instructed rather than shipping a non-doc approximation.

### brownie  [needsEngine -> src/engine/core/play.ts (+ src/engine/effects/select.ts)]
CONFIRMED tautology: selectInstancesWith opens with `if (sel.self) return [ctx.sourceIid]` and never reads zone/pick/count, so both `any:` branches returned 1 id and atLeast:1 always passed — every Brownie shed Flimsy and persisted forever (a free repeatable +1 Action/+2 Cards on a (1)-cost token). Not expressible card-side either way: without self:true the selector reads a hand the card has already left, because playCard step 2 moves it to 'play' before effects run at step 5. Engine fix: record the pre-move hand index and hand length in playCard step 2 (onto ctx.vars or inst.counters) and expose them so the card can gate on `handIndex == 0 || handIndex == handSizeAtPlay - 1`. INTERIM CARD-ONLY IMPROVEMENT APPLIED: deleted the always-true conditional so Brownie ships as plain Flimsy — the doc row's base state — and set text to 'Flimsy. +1 Action, +2 Cards.'. Restore the edge-of-hand clause (and the doc's wording) once hand position is readable. Left subsystems: ['S-TOKEN','S-PERSIST'] as a marker.

### loaf_of_bread  [needsEngine -> src/engine/core/play.ts (+ src/engine/types.ts Selector.pick)]
CONFIRMED: `{ op: 'playCard', target: { zone: 'hand', count: 2, pick: 'choose' } }` lets the player play any two cards; opkit's 'choose' branch re-selects with pick and count stripped (the whole hand) and prompts min=max=2. Nothing reads hand position, so the printed adjacency constraint is gone. Same hook Brownie needs: capture the source's hand index before playCard step 2, then select index-1 and index+1 (a `pick: 'adjacent'` Selector option, or the index passed through ctx.vars). CARD LEFT ALONE deliberately — there is no card-only behavioural improvement, and the printed text still matches the doc row, so only the effect node needs to change when adjacency lands. (The alternative the finding offers — rewriting the text to 'choose two cards in your hand and play them' — would make the catalog diverge from A.16, so I did not take it.)

### performance_enhancing_cookie  [needsEngine -> src/engine/core/play.ts]
CONFIRMED dead field. consumePlayMods (play.ts:167) sums `mod.buffTimes` into ResolvedMods.buffTimes and then playCard never reads it — steps 3-6 use only grantKeywords, multiplier/multiplyStats, appendEffects and absorbInto; buy.ts handles only appliesTo:'buy'; multiplierFor in ops/replay.ts reads only mod.multiply. So a (5)-cost Epic pays +1 Action and a Crumb. Fix in playCard between step 3 and step 4: for mods.buffTimes iterations call applyBuff(s,'instance',iid,1,null,rng), and -1 for mods.nerfTimes, taking/committing the rng cursor the way callApplyBuff does in effects/ops/buff.ts:25-48. applyBuff on scope 'instance' writes inst.statDelta, which effectiveStats already reads. THE CARD DEFINITION IS CORRECT AS WRITTEN — no card change made or needed.

### performance_enhancing_crumb  [needsEngine -> src/engine/core/play.ts]
CONFIRMED, same unread mods.buffTimes as the Cookie, and here it is the card's entire effects array, so the card reduces to 'Flimsy, +1 Action'. It also blanks the second half of Performance Enhancing Cookie, which mints this card into the GY. Same one-site fix (honour mods.buffTimes/nerfTimes in playCard before applyStats). CARD DEFINITION IS CORRECT AS WRITTEN — no card change made or needed.

### distilled_gluten  [needsEngine -> src/engine/effects/select.ts]
CONFIRMED per-player vs pooled bug. selectInstancesWith resolves `who` to a player list and then POOLS every player's zone into one candidate array (`for (const zone) { for (const pid) candidates.push(...) }`), sorts by instanceCost for pick:'cheapest' and returns matched.slice(0, 1) — one instance overall, wrong even at 2 players, where the doc wants the cheapest card in EACH hand. Contrast opCreateCard/opGainCard/opRecruit, which all loop `for (const pid of players)` and apply count per player. Fix: apply pick/count once per resolved player when who is 'eachPlayer'/'eachOpponent' (the idiom recurs across the catalog, so this covers the others). No card-level workaround exists — forEach iterates instances, not players, so the pooling reappears inside its `over` selector. CARD LEFT ALONE: the node and the text are both right once the selector resolves per player.

### distilled_potato  [needsEngine -> src/engine/effects/select.ts]
CONFIRMED, same pooling path: `{ who: 'eachOpponent', zone: 'hand', count: 1, pick: 'random' }` concatenates every opponent's hand, shuffles, and takes one — so at 3+ players exactly one opponent is hit and the rng picks which, instead of one card from each. Correct only at 2 players. Same per-player fix in selectInstancesWith. CARD LEFT ALONE — I considered `count: { expr: 'playerCount - 1' }` as a card-only stopgap and rejected it: it still draws from the pooled union (one opponent can lose two cards and another none) and would force the printed line away from the A.16 wording.

### cornucopia  [needsEngine -> src/engine/types.ts (EXPR_VARS) + src/engine/effects/context.ts (buildVars)]
CONFIRMED wrong condition. `not: { expr: 'cardsGainedThisTurn' }` tests gains, not Buys, and p.cardsGainedThisTurn is bumped by opCreateCard, both opGainCard branches and opCopyCard as well as by a purchase — so any token this deck mints (Crumb, Grapes, Fruit Basket, Hearty Meal, House Party) blocked a (6)-cost Epic with zero Buys spent. PlayerState.buysUsedThisTurn exists (types.ts:548, written buy.ts:162, reset turn.ts:118) but is absent from EXPR_VARS, so no expression can read it. Engine fix: add 'buysUsedThisTurn' to EXPR_VARS and fill it in buildVars alongside the other per-turn counters, then gate on `{ not: { expr: 'buysUsedThisTurn' } }`. CARD-ONLY IMPROVEMENT APPLIED: gate is now `{ expr: 'buysRemaining >= 1' }` and the text reads 'if you still have a Buy left'. endTurn fires endOfTurn triggers at step 1, before buys are zeroed at step 4, so buysRemaining is live there. It is exact except on a turn where a card granted an extra Buy AND one was spent. Restore the doc's 'if you used no Buys' wording once buysUsedThisTurn is an EXPR_VAR.

### rct_cn  [needsEngine -> src/engine/types.ts (NextCardMod/CardInstance) + src/engine/effects/select.ts + src/engine/core/play.ts]
CONFIRMED on both counts and LEFT ALONE — no card-only fix exists. (1) 'Next card played gains the CN tag' is encoded as `{ op: 'nextCardModifier', mod: { buffTimes: 1, ... } }`, which is not a tribe grant at all: NextCardMod has grantKeyword but no subtype field, and CardInstance has addedKeywords/removedKeywords but no subtype override, so nothing about the next card becomes CN. (2) That mod is itself dead — playCard never reads mods.buffTimes (see the two Performance Enhancing findings). Fix: add `grantSubtype?: string` to NextCardMod plus `addedSubtypes` on CardInstance, honoured by matchesFilter's subtype test in select.ts, then REPLACE the node with `{ op: 'nextCardModifier', mod: { grantSubtype: 'CN', appliesTo: 'play', uses: 1 } }`. IMPORTANT: replace, do not merely activate. A.22 gives RCT CN no next-card buff at all (its buff is the allCopies node, which works), so turning on mods.buffTimes without swapping this node would hand the card an effect the doc does not grant.

### cncias  [needsEngine -> tools/validate-cards.ts + test/catalog-integrity.test.ts]
Follow-on from the CNcias fix, and NOT mine to edit (tools/ and test/ are off-limits). Both the validator (validate-cards.ts:217, the createCard/addToPileTop defId check) and test/catalog-integrity.test.ts:165 reject the literal '$discovered' as an unknown defId. Four cards from four different agents now trip it: cncias, kwzki_high_council_consultant, yuyas_embrace, mathemagiks. Both checks need to skip DISCOVERED_SENTINEL / SELECTED_SENTINEL (exported from src/engine/effects/ops/choices.ts via effects/index.ts). Whoever owns tools/ and test/ should whitelist the two sentinels; the card data is correct.

### zephrys  [needsEngine -> src/engine/effects/ops/choices.ts]
PARTIALLY FIXED card-side: `then` set to [] so the pick is now the card you receive (was a fresh knownUniverse re-roll). STILL ENGINE: the SB-40 heuristic never runs. opDiscover calls samplePool unconditionally at choices.ts:76, and `simSubstitutes` (discovery.ts:21-28) plus perfectCardFor (engine/meta/sim.ts:150) have no importer anywhere in src/, test/ or tools/, so the three offered cards are plain weighted random pulls. Fix: before samplePool, look up s.instances[item.sourceIid].defId in a substitute table and use the returned ids as the offered options. The table must move into the engine (or be registered from the card layer at load) — cards importing engine is the wrong direction for the engine to consult it. I softened the file header comment, which claimed the interpreter already calls the hook.

### second_time_around  [needsEngine -> src/engine/effects/ops/choices.ts]
PARTIALLY FIXED card-side: `then` set to [] so the picked card is the card gained. STILL ENGINE: identical to Zephrys — simSubstitutes.second_time_around (winningDeckFor(...).slice(0,3), engine/meta/sim.ts:184) is never consulted, so the three options are random rather than the scorer-ranked picks SB-40 specifies. Same opDiscover hook fixes both.

### what_is_love  [needsEngine -> src/engine/types.ts (new op) + src/engine/systems/fuse.ts]
PARTIALLY FIXED card-side: `then` set to [] so BOTH picked cards now enter hand (localResume's discover default loops over every chosen option), instead of one independently sampled random card per prompt. STILL ENGINE: nothing fuses. There is no `fuse` op in the EffectNode union, fusedDefinition (systems/fuse.ts:50) is reachable only from this file's unimported fuseForCard helper, and CardInstance.fusedFrom is never written. Needs an op such as {op:'fuse', components:[...], to:'hand'} that runs the SB-13 arithmetic, registers the produced definition so getCard resolves it, and creates one instance with fusedFrom set — plus a way for both picked defIds to reach that op in one node (today `then` runs once per chosen card, so a two-card fuse cannot be expressed). Keep the outer repeat x2 for the printed 'twice'. Text left describing the doc row.

### matchmaker  [needsEngine -> src/engine/types.ts (new op) + src/engine/systems/fuse.ts]
CONFIRMED, card left alone — no card-only fix exists. {op:'copyCard', target:{zone:'library', count:2, pick:'random'}, to:'library'} clones both sources and leaves the originals in place (ops/movement.ts:165-190), so a card printed as deck compression inflates the Library by 2 duplicates; nothing merges and no fusedFrom is set. The 'non-fused' restriction is also inexpressible: CardFilter (types.ts:238-256) has no fusedFrom/fused predicate. Needs the same fusion op as What is Love?, consuming the 2 selected non-fused library instances and replacing them with one fused instance, then shuffling.

### freaky_phil  [needsEngine -> src/engine/types.ts (new op) + src/engine/systems/fuse.ts]
CONFIRMED, card left alone — Matchmaker's defect with count 3. copyCard adds three duplicates to the Library instead of fusing three cards into one. Same fusion op and same missing non-fused filter; cost summed and capped at 20 per SB-13.

### frankenstein  [needsEngine -> src/engine/core/buy.ts]
CONFIRMED, card left alone — all three sub-defects are engine-side. (1) peekBuyMods/consumeBuyMods (buy.ts:35-65) read only costDelta, costFloor and buyTo; appendEffects is spliced into a card body on the PLAY path only (play.ts:273), so the 50% refund never pays out. (2) consumeBuyMods decrements uses on EVERY appliesTo:'buy' mod at the first purchase and peekBuyMods already returned buyTo:'hand' for it, so the second mod also lands on purchase 1 — NextCardMod (types.ts:383-409) has no ordering/skip field, so the sequencing cannot be authored card-side. (3) 'fuses with the previous purchase' needs the fusion op above. A refund also needs selfCost bound to the purchased card, not to Frankenstein; there is no expr var for the last purchase's cost either (EXPR_VARS, types.ts:200-225), so no delayed-effect workaround exists. Left costDelta: 0 in place as a harmless no-op rather than touch a non-finding.

### infinite_realities  [needsEngine -> src/engine/effects/ops/movement.ts (or src/engine/effects/pools.ts)]
CONFIRMED, card left alone. opCreateCard loops `count` times calling resolveDefIdSpec (movement.ts:66-70), so the 10 replacement cards are 10 independent weighted random pulls, and simSubstitutes.infinite_realities (winningDeckFor, engine/meta/sim.ts:184) is imported by nothing. A (20) Mythic hands out a random pile. No card-only route to a curated deck exists: no op materialises a caller-supplied list of defIds. Fix: let pool resolution consult a substitute registered against the source card's defId, or add an explicit op for the curated deck; keep the trash / shuffle / draw-5 frame as is.

### miracle_moonfall  [needsEngine -> src/engine/effects/ops/shop.ts (+ src/engine/shop/cost.ts)]
CONFIRMED, card left alone. opAddToPileTop writes costOverride onto the PILE (shop.ts:252) and nothing ever clears it; pileCost (select.ts:337-341), effects/runtime.ts:149 and shop/cost.ts:102 all short-circuit on it, so after Moonfall three Draft piles are (0) for every player for the rest of the match. There is no card-only fix: there is no per-instance cost field, and transform's {costDelta} swaps to a DIFFERENT card at that price (movement.ts:216-217) rather than repricing anything. Dropping costOverride would leave the Fragment at its printed (6) (cosmic.ts:27), contradicting the B.1 row's '(0)-cost Lunar Fragment', so I left the data matching the doc. Fix: scope the override to the added instance, or record the overridden iid on the pile and clear pile.costOverride in buyCard when that instance leaves the top. Same idiom needs the same repair in chron_cache and supernova (economy/shop-manip.ts:838, 865) — another agent's file.

### mewing  [needsEngine -> src/engine/effects/triggers.ts]
'add a Felinor to GY that plays it when trashed' cannot be authored: fireEvent iterates def.triggers only, instance `extraEffects` are appended on play (core/play.ts) and never on trash, and a static token def cannot carry an arbitrary discovered defId. Needs either instance-level triggers or a counter/field the trash path reads to play a carried defId. Card-only rendering shipped: the discovered Action and a plain Felinor both go to your GY.

### mewing  [needsEngine -> tools/validate-cards.ts]
TOOLING, not engine, and I am not allowed to touch it: `{op:'createCard', defId:'$discovered'}` — the route the brief told us to use — is rejected by validate-cards.ts (line ~215) and by test/catalog-integrity.test.ts B97, both of which check a string createCard defId against the registry with no sentinel exemption. mewing is one of NINE cards now failing this way (the others are kwzki_high_council_consultant, yuyas_embrace, cncias, jeweled_scarab, golden_scarab, empyreal_scarab, mathemagiks, homebrew, from other agents). Both checks need to skip defIds starting with '$'.

### took_him_to_the_jo  [needsEngine -> src/engine/effects/select.ts]
selectInstancesWith concatenates every resolved player's zone into one candidate list before applying `count`, so 'the top 2 of each opponent's Library' takes 2 cards TOTAL once there are 3+ players. Needs `count` applied per resolved player. The card is otherwise correct and is exact in 2-player.

### recurring_felinor  [needsEngine -> src/engine/effects/ops/movement.ts]
'goes to the CURRENT player's GY' is unreachable from card data: MoveTo has no `who` field, trashInstance preserves i.owner, fireEvent sets the trigger player to i.owner ?? actor, and opMoveTo resolves an OWNED destination as i.owner ?? item.player — all three land on the existing owner, never state.activePlayer. Needs either the trashing actor threaded through the onTrash context or an owner override on moveTo. Card left as authored.

### felinor_factory  [needsEngine -> src/engine/core/turn.ts]
'if you trashed a Felinor this turn' is checked as {zone:'trash', filter:{subtype:'Felinor'}}, and selectInstancesWith reads the trash with player=null, so it sees every Felinor any player ever trashed — permanently true from the first Flimsy Felinor played. There is no trashed-this-turn EXPR_VAR and countIn('trash', felinor) is built with player=null too. Needs a per-player counter stamped on trash and reset in the turn cycle, then {expr:'felinorsTrashedThisTurn >= 1'}. No card-only improvement exists, so the card is untouched apart from the subtype narrowing.

### box_of_kitties  [needsEngine -> src/engine/core/turn.ts]
Same unowned, unturn-scoped trash read as felinor_factory for the +1 Buy, and it resolves immediately after the two Felinors are added, i.e. before the player could trash one. The printed clause is forward-looking ('if you trash one this turn'), so it wants the same per-player counter checked at end of turn. Card untouched apart from the subtype narrowing.

### book_of_felinors  [needsEngine -> src/engine/effects/select.ts]
Same per-player `count` gap as Took Him to the J'O: with 3+ players the eachOpponent discard still takes 2 across all opponents rather than 2 each. forEach binds instances, not players, so the self/eachOpponent split I shipped is the ceiling for card data.

### book_of_blood  [needsEngine -> src/engine/effects/triggers.ts]
The 'when trashed, add an original copy to their hand' half of the row cannot be carried: opSetKeyword only writes addedKeywords/removedKeywords and fireEvent resolves triggers off def.triggers, so a marked instance has nowhere to hold the rider. Smallest fix is a recognised counter (counters.bookOfBlood = 1, set alongside the keyword) honoured by trashWithTrigger, creating a fresh copy of that defId in the owner's hand. Card left as authored; I added a comment above the effect recording the omission rather than promising it in `text`.

### outbreak  [needsEngine -> src/engine/types.ts (CardFilter) + src/engine/effects/select.ts (matchesFilter)]
'gain any shop card whose tokens exceed its cost' has no primitive. CardFilter offers only `plagued?: boolean`, so the shipped filter is 'has at least one token' and — because the node above just tokened two random pile tops — a qualifying target always exists, making it a free `pick:'mostExpensive'` Shop gain every play. Needs e.g. `plagueOverCost?: boolean` evaluated as (counters.plague ?? 0) > instanceCost(state, iid), then `from:{shop:'all', filter:{plagueOverCost:true}, pick:'mostExpensive', count:1}`. Card left alone: every card-only approximation either keeps the break (cost:{lte:N}) or bricks the card (cost:{eq:0} — no purchasable Shop pile costs 0).

### plandemic  [needsEngine -> src/engine/effects/ops/misc.ts (opPlague) + src/engine/types.ts (plague node)]
'Double all Plague Tokens' is implemented as two `{op:'plague', amount:1}` nodes; opPlague only ever does bumpCounter(..., 'plague', amount), so a 3-token card goes to 4, not 6. Needs a doubling mode (e.g. `mode:'double'` writing counters.plague *= 2) on the plague op. The forEach + {expr:'selfCounter'} workaround is unsafe for the reason in the juhan_wet_market entry.

### plandemic  [needsEngine -> src/engine/types.ts (CardFilter) + src/engine/effects/select.ts (matchesFilter)]
The copy clause's 'cards whose tokens equal their cost' gate is absent — same missing primitive as Outbreak. Needs `plagueEqualsCost?: boolean` ((counters.plague ?? 0) === instanceCost(state, iid)); today `filter:{plagued:true}` + `pick:'mostExpensive'` reliably copies the most expensive plagued card in any graveyard at the table into the caster's hand, free, every play. The printed 'minimum 1' should then become an explicit else branch rather than a side effect of an unfiltered pick.

### juhan_wet_market  [needsEngine -> src/engine/effects/context.ts (buildVars, lines ~141-153)]
`selfCounter` is the SUM of every numeric counter on the instance and only 'counter'/'uses'/'charges' are treated as named, so it includes the engine's own inst.counters['playCount'] (src/engine/core/play.ts:242, bumped before the body runs). Juhan draws tokens + times-played, and the view renders {plague} from counters.plague alone, so the printed number and the drawn number disagree. Fix in buildVars: exclude bookkeeping keys ('playCount', 'podChain', 'trg:*', 'trashSurvivals', 'promptTurn', 'promptsThisTurn') from counterTotal, or add 'plague' to the named-key list. plague_charger's +X VP in the same file reads the identical expression and drifts the same way. No card edit made — the finding's own fix is engine-side and repairs both cards with none.

### crop_dusting  [needsEngine -> src/engine/types.ts (Selector) + src/engine/effects/select.ts (selectInstancesWith shop branch)]
The '+2 Money if any had none' gate tests every instance in every Shop pile (zoneIds returns each pile's whole cards array), but opPlague only ever tokens pile tops, so buried cards are permanently unplagued and the condition can never fail — the +2 Money is unconditional. Condition.has takes a Selector, not a PileSelector, so there is no way to say 'pile tops only' today. Needs e.g. `pileTop?: boolean` on Selector, honoured by pushing only pile.cards[0] in the shop branch, then if:{has:{target:{zone:'shop', pileTop:true, filter:{plagued:false}}, atLeast:1}}. Card left alone; the node ordering (check, then plague) is already correct.

### patient_zero  [needsEngine -> src/engine/types.ts (CardFilter) + src/engine/effects/ops/movement.ts / src/engine/core/buy.ts]
'if any card gained this turn had a token' is implemented as 'any plagued card anywhere in your GY', which latches on permanently the first time one lands there. There is no gain timestamp on an instance. Needs counters['gainedOnTurn'] = state.turn stamped in opCreateCard / opGainCard / buyCard plus a `gainedThisTurn?: boolean` CardFilter clause compared against state.turn, then filter:{plagued:true, gainedThisTurn:true}. Note for whoever does this: the new counter must be excluded from selfCounter's counterTotal (see juhan_wet_market) or it will corrupt Plague Charger's VP.

### plague_crawler  [needsEngine -> src/engine/effects/triggers.ts (fireEvent) + src/engine/types.ts (Who)]
The +2 Cards go to the Crawler's owner, not the placer: fireEvent builds the trigger context with `const owner = i.owner ?? actor` and passes player: owner, and `actor` is only consulted to gate onOpponentBuy/onOpponentPlay. onPlagueAdded is a SELF_EVENT with no `zones` on the trigger, so a Crawler in your GY fires when an opponent's Living Bomb or Plandemic tokens it and YOU draw 2 on their turn. Needs the actor threaded into the trigger context plus a `who:'actor'` case in Who. Card left completely alone on purpose: its text and the doc row both say 'the placer', so the printed line is already correct and will become true the moment the engine is fixed — editing the text now would only have to be reverted.

### jalshi  [needsEngine -> src/engine/types.ts (EXPR_VARS) + src/engine/effects/context.ts (buildVars)]
The second mode pays 1 Money per plagued CARD, not per token removed: `countIn(hand, plagued)` counts matching instances, then opRemovePlague zeroes each whole counter, so a card carrying 3 tokens (Plague Charger places 3; Juhan accrues 2 per discard) is destroyed for 1 Money. The card's own text says 'X = tokens removed'. Needs a variable that sums counters.plague across the hand (e.g. plagueInHand) in EXPR_VARS/buildVars, then amount:{expr:'plagueInHand'}. The forEach + selfCounter route is unsafe until selfCounter stops summing playCount.

### the_mob  [needsEngine -> src/engine/core/buy.ts (peekBuyMods / consumeBuyMods) + src/engine/core/turn.ts]
Complete no-op, confirmed. The card pushes a NextCardMod with appliesTo:'buy' carrying appendEffects, and the buy path copies only costDelta/costFloor/buyTo; the sole reader of appendEffects is consumePlayMods, which skips any mod whose appliesTo is not undefined/'play'/'action'. Needs buy.ts to collect and run buy-scoped appendEffects in the buyer's context after the bought card lands. Bound the duration at the same time: nextCardMods are cleared neither in resetTurnStats nor in endTurn, so uses:99 also outlives 'this turn'. No card-only route exists — onBuy/onGain are SELF_EVENTS that fire only on the bought instance, so a trigger on The Mob can never see someone else's purchase, and there is no non-self buy event in TriggerEvent. (An end-of-turn `count:{expr:'cardsGainedThisTurn'}` approximation would count every created token — Eggs, Soul Shards — so it is worse than doing nothing.) Card left untouched.

### skull_of_juldan  [needsEngine -> src/engine/core/play.ts (stat draw) / src/engine/effects/ops/cards.ts (opDraw)]
RESIDUAL after the card-only fix listed under `fixed`. The loop still cannot know WHICH cards the printed +3 Cards drew — it approximates with the 3 most recently added (0)-cost cards in hand. To be exact the engine must stamp or bind the drawn instances (e.g. counters['drawnOnTurn'] = state.turn plus a filter clause, or a `{op:'draw'}` variant that exposes its result as a selectable set), after which the card should become forEach over exactly those. Note the same caveat as patient_zero: any new bookkeeping counter must be excluded from selfCounter's counterTotal.

### mathemagiks  [needsEngine -> src/engine/types.ts (NumericFilter) + src/engine/effects/pools.ts]
The printed pool is 'costing (handSize)'. CardFilter.cost is a NumericFilter of literals only, so a pool cannot scale with an Amount/expression. Kept the existing cost:{lte:10} pool and made the text honest instead. Needs an Amount-capable cost filter (evaluated against the ctx at sample time) before the doc row can be expressed.

### homebrew  [needsEngine -> src/engine/core/play.ts:278-281 / a new absorb op in src/engine/effects/ops/]
'permanently add its effect to this card' has no data route: inst.extraEffects is written only by play.ts's absorbInto path, and no op appends a definition's effects to a live instance. Card-only improvement applied so the pick at least matters: `then` now starts with {op:'createCard', defId:'$discovered', to:'hand'} before the brewed counter and the transform-into-upgrade, and the text says so. A real fix wants {op:'absorbEffects', from:<defId>, into:{self:true}} appending getCard(defId).effects to extraEffects — then restore the printed text.

### infini_scepter  [needsEngine -> src/engine/core/play.ts (consume mods.bind) + src/engine/effects/ops/auras.ts:56-68]
Card-only improvement applied: dropped the inert {mod:{bind:'oathboundMemory'}} node and the bindTo:'nextPlayed' form (whose early return shadowed the auraId branch, so manifestAura was never called at all) in favour of {op:'manifestAura', tier:'celestial', auraId:'oathbound_memory', who:'self'}; text updated. The aura now exists but is UNBOUND, so meta/auras.ts:196 skips the bound branch and the aura's own data trigger hands out a random Known Universe Temporary card. To restore the printed 'the next Action played is bound', playCard must consume mods.bind === 'oathboundMemory' and call manifestAura(s, player, 'oathbound_memory', 'celestial', inst.defId) — meta/auras.ts:97-102 already takes boundDefId — and ops/auras.ts must check node.auraId before the bindTo early return.

### hivemind  [needsEngine -> src/engine/effects/ops/timing.ts:70]
Card left alone: absorbInto:'self' is a sentinel string, not an InstanceId, so s.instances['self'] is undefined and play.ts:278-281 never pushes. There is no card-only route — instance ids are minted at runtime. One-line fix at the point the mod is copied: `if (copy.absorbInto === 'self' && item.sourceIid) copy.absorbInto = item.sourceIid;` (or an explicit `absorb:true` flag). Until then the Flimsy grant works and the headline clause is silent while the {absorbed} counter keeps climbing.

### pointer  [needsEngine -> src/engine/core/play.ts + src/engine/core/zones.ts:202 + src/engine/effects/ops/timing.ts:71]
Card left alone (every card-only option either blanks a 4-cost card or invents non-doc behaviour, and removing the bind would strand the card if the engine later consumes it). mods.bind === 'pointer' is collected and never read, so no binding exists; per SB-7, playCard must record the partner iid on both instances and trashInstance must Mutilate the partner. URGENT collateral: the autofill at ops/timing.ts:71 fills absorbInto from ANY bind, so today Pointer silently behaves as a persistent Hivemind — narrow that autofill when the bind is consumed.

### pashes_the_pie_rat  [needsEngine -> src/engine/types.ts (TriggerEvent) + src/engine/core/play.ts:284]
Partial card-only fix applied: zones ['library','gy','hand'] → ['play'], because 'onPlay' only ever reaches the just-played instance and that instance is always in 'play' — the trigger was unreachable on every path. It now fires on Pashes' own play (it is itself a (1)-cost Legendary, so the condition holds) and Recruits another copy; text updated to say so. The printed 'whenever you play ANY (1)-cost Legendary' needs a self-inclusive table-wide event ('onAnyPlay' dispatched over all owned instances, with the zones list restored so a Pashes in the Library can answer).

### the_eternal_show  [needsEngine -> src/engine/effects/ops/replay.ts:127-131]
Card left alone. The condition can only test that The Past and The Future are somewhere in your play area; SB-24 requires the actual The Past → The Future → this-instance copy chain, and the cycle detector (the __playing: guard) only writes a replayCycleBlocked log line and leaves no state a Condition can read. resolveCardPlay needs to record the blocked chain (e.g. a counter on the Eternal Show instance) before the payoff can be gated on it.

### mathemagiks, homebrew (and 12 cards owned by other agents)  [needsEngine -> tools/validate-cards.ts:216 and test/catalog-integrity.test.ts (B97)]
Both the validator and the B97 tests reject the sanctioned '$discovered' sentinel: 'createCard references unknown defId "$discovered"' (14 cards, incl. cncias, the three scarabs, yuyas_embrace — pre-existing before my edits) and the same for transform (model_citizen). The sentinel is substituted at resume time and never reaches the registry, so the defId checks must skip DISCOVERED_SENTINEL / SELECTED_SENTINEL. I cannot edit tools/ or test/. Nothing else in the suite is red from my files: the other failure, meta-auras 'B80: aura triggers fire on top of the reset', comes from src/cards/advanced/auras.ts, which another agent is editing.

### in_too_deep  [needsEngine -> src/engine/effects/ops/misc.ts (opQuestProgress) + src/engine/meta/quest.ts]
Part (a) of the finding is now stale: aura triggers DO fire — fireOwnedTriggers calls fireFieldTriggers, buy.ts and play.ts call it for onBuy/onGain/onPlay, and fireEvent walks p.field. Part (b) stands: opQuestProgress only does progress[key] += amount and seeds p.quest = {floor:'f1'}, an id getFloor() does not know (floors are '1','2a',...). questProgress/settleFloor/questStartOfTurn in meta/quest.ts are exported from meta/index.ts and called by nothing in src/, test/, api/ or tools/. So no floor ever completes and no reward is ever paid. The quest has to be driven from the buy/play/trash/draw/end-of-turn paths, not from card data. Card left alone: the two onBuy conditions are also mis-shaped (they ask 'do I hold any (8)+ card / any Diamond', not 'is the card I just bought one'), but an aura trigger's context has sourceIid === null (fireAuraTriggers -> makeContext(player, null, depth)), so selfCost is unavailable and no card-only condition can name the bought card.

### shooting_star  [needsEngine -> src/engine/meta/auras.ts (manifestAura)]
A Hypercelestial aura's `effects` array is never executed. activateAura is the only caller of def.effects and canActivateAura rejects any non-heroic aura with no activationCost; manifestAura only pushes an AuraInstance and logs. The fix is to resolve def.effects once on the p.field.push path (not on the two 'auraRefreshed' early returns, so re-manifesting cannot re-fire). No card-only route: giving it an activationCost would make the three Celestials a repeatable once-per-turn activation instead of 'on summon', which is worse than inert.

### mycology  [needsEngine -> src/engine/types.ts (EffectNode) + src/engine/systems/fuse.ts]
The Discover half is fixed (see `fixed`), but 'Fuse them' cannot be expressed: there is no {op:'fuse'} in the EffectNode union, and fusedDefinition/fuseForCard in systems/fuse.ts have no caller anywhere. The clause is off the aura text until a fuse node exists that can collect a discover's picks. This gap is catalog-wide, not specific to Mycology.

### call_to_chaos  [needsEngine -> src/engine/effects/select.ts (selectInstancesWith) + src/engine/types.ts (Selector)]
Branch 3, 'All players trash 5 random cards from their deck', trashes 5 table-wide from one merged, once-shuffled candidate list, so a 4-player game loses 5 cards instead of 20 and all 5 can come out of one deck. Needs an explicit per-player flag on Selector (e.g. perPlayer: true) that runs match/pick/slice once per resolved player; changing the pooled semantics wholesale would silently rewrite ~30 other selectors that legitimately want a table-wide total. Card left alone — count:{expr:'playerCount * 5'} would get the total right but could still take every card from a single player, which is worse than the current shortfall.

### call_to_chaos  [needsEngine -> src/engine/effects/ops/control.ts (opRandom)]
Branch 27's doc row wants a Discover among Call to Chaos effects. {op:'random'} has no affordance for offering branches as prompt options and {op:'choose'} takes a static option list, so a genuine 'sample 3 branch indices and present them' needs an engine addition (e.g. a `pick` mode on opRandom that suspends with a choose prompt over N sampled branches). SB-42's 60-prompt-per-turn ceiling already guards the recursion. I corrected the printed line instead so the promise matches the roll.

### aura_gambit  [needsEngine -> src/engine/systems/steal.ts (stealInstance) + src/engine/types.ts (Who)]
The 'when trashed, give it to [you]' half is unimplemented and unimplementable in card data. The counters:{auraGambit:1} marker is read nowhere, and warhero_token (src/cards/tribes/tokens.ts, not my file) has no triggers. The return needs an ownership transfer to the GRANTING player: opMoveTo's `who` resolves against the trigger's own player, which for a trigger on the gifted token is the opponent, so who:'self' returns it to them. Either expose stealInstance() as an op (it exists with no callers) or add a Who naming the instance's granting player, then hang it on onTrash of a dedicated gambit token def — and note the gift is Flimsy, so onTrash must also fire from the Flimsy cleanup path in play.ts. Card left untouched rather than shipping a 2-player-only approximation via who:'randomOpponent'.

### tanyays_unstable_element  [stillBroken]
"You can't draw more this turn" cannot be expressed in card data. The multiply:0 nextCardModifier that used to stand in for it was worse than nothing (consumePlayMods skips a zero multiplier while spending a use; multiplierFor ignores multiplyStats and zeroed Money/Buys/Actions/VP for the next 99 plays; resetTurnStats never cleared it). It needs a per-turn no-draw flag drawCards/opDraw honour and resetTurnStats clears. Left in the safest state: the modifier is gone, the rider is off the text, and the reason is on the card.

### sleepy_joe_bider  [stillBroken]
The printed upside - ending the turn without discarding - needs engine work no card data can do: counters.endTurnRequested is written by opEndTurn and read nowhere in src/, test/ or tools/, so nothing advances the turn, and core/turn.ts endTurn discards the hand unconditionally with no skip-discard flag to honour. The card is left doing what it can (zeroing Actions and Buys) with text that says only that.

### save_for_later  [stillBroken]
Cross-file: the `boxed` counter is never cleared. permanent_hand_box MOVES the stored instances back to hand (src/cards/advanced/tokens.ts), and they keep boxed=1 for the rest of the game, so a later Keyhole / Essential Oils / Counting Cards treats them as still-stored and leaves them behind in `aside`. Fix belongs in tokens.ts - decrement the counter on the way out, e.g. an `{op:'addCounter', target:{who:'self', zone:'hand', filter:{counter:{key:'boxed',gte:1}}}, key:'boxed', amount:-1}` after the move. Repackage is unaffected: it stores copies the Temporary box only ever copies out.

### mother_witch  [stillBroken]
Confirmed real, and not fixable in card data. opRecruit calls shuffleWithTrigger on the source zone unconditionally (movement.ts:278) and shuffleZone shuffles a hand of 2+ cards (zones.ts:287-294), so {op:'recruit', zone:'hand', who:'eachOpponent'} scrambles hand order that zones.ts:155-157 documents as meaningful (B13). Unlike the library case there is no SB-2 anti-leak rationale for a hand. But no other op takes one card per player: a Selector's count is a table-wide total; {op:'choose', who:'eachOpponent'} does not loop players (opChoose picks ONE chooser via rng.pick and runs the branch in the RESOLVER's frame, choices.ts:176-180); selectCards and discover prompt the resolving player, which would leak opponents' hands; delayed would move the effect to a different turn. Left the card in its current state - it does the right primary thing (each opponent turns one card of their own into a Cursed Pig) and only hand order is collateral - with a comment naming the cost, plus the boxed guard on both aside reads. Engine fix: an opt-out on recruit's shuffle (e.g. shuffleAfter?: boolean, default true) or a real per-player loop op.

### hired_shrimp  [stillBroken]
Confirmed real and pre-existing (audit-findings/archetypes__pvp.md rates it [high] <dead-filter>). matchesFilter (select.ts:182) does literal def.name !== filter.name, so filter:{name:'wordsFewerThan:16'} matches nothing and the (5) Legendary trashes nothing. CardFilter (types.ts:240-263) has no word-count, substring or regex axis - the new counter axis does not help - and the frozen wordCount: 16 is read only by fuse.ts:111 and tools/export-cards.ts:34, never by a selector. There is no card-data expression of this card. Left in the safest state: the inert filter stays, because deleting it would make the selector match EVERY card and trash every opponent's whole hand. The text stays faithful to doc line 969 rather than being rewritten to describe a blank. Engine fix, as the audit proposed: add wordCountLt?: number to CardFilter, resolved in matchesFilter/matchesDefFilter against the target definition's wordCount (falling back to counting whitespace-separated tokens of def.text), then author filter:{wordCountLt: 16}. The same missing facility keeps profe_yates_unleashed's name:'/[moseMOSE]/' dead.

### cornucopia  [stillBroken]
Real and not fixable in card data. The doc gate ('End of turn: if you used no Buys', jlore_jlards_gameplay.md:1085) needs p.buysUsedThisTurn, which exists at types.ts:562 and is written at core/buy.ts:162 but is absent from EXPR_VARS (types.ts:196-223) and buildVars (effects/context.ts) — and Condition (types.ts:419-430) offers no buys-used primitive, so no card node can express it. EXPR_VARS/context.ts are engine files owned by another agent. Left in the safest state: condition `{expr:'buysRemaining >= 1'}` with text truthful to it ('if you still have a Buy left'). Reverting to `{not:{expr:'cardsGainedThisTurn'}}` is strictly worse — opCreateCard/opGainCard/opCopyCard all bump cardsGainedThisTurn, so every minted token blanks a (6) Epic in the deck it was designed for. One correction to the review: the Prophet path is not a second divergence. core/buy.ts:150-156 skips `p.buys -= 1` AND `p.buysUsedThisTurn += 1` together (B59), so the prescribed `buysUsedThisTurn == 0` gate would also fire after a Prophet purchase. The only genuine divergence is a turn where a card granted an extra Buy and one was spent. Engine fix needed: add 'buysUsedThisTurn' to EXPR_VARS and `vars.buysUsedThisTurn = p.buysUsedThisTurn` in buildVars beside vars.buysRemaining (context.ts:121), then set condition `{expr:'buysUsedThisTurn == 0'}` and restore the doc text. The in-file comment at food.ts:634-643 records this.

### twenty_fifth_hour  [stillBroken]
The card can no longer show progress toward the gate, because no `{...}` token resolves to `state.round`. renderCardText (src/engine/view.ts:75-99) handles name/cost/plague, instance counters, secrets, statDelta, stats, playCount/selfPlayCount and turn, then returns '0' for anything else — so '{round}' or '{roundNumber}' would print '0'. Fixing this needs one line in view.ts (`if (key === 'round') return String(state.round);`), which is outside my files. I chose no number over a wrong one; the gate itself is now correct.

### astrologist  [stillBroken]
The doc row's full scope (jlore_jlards_gameplay.md:1278, 'every even-numbered Astrologist trashed this game') is still unreachable in card data. Two gaps: the Flimsy play-cleanup trash raises no 'onTrash' event, so a trigger-based count cannot see the ordinary case at all; and there is no table-wide trash event carrying a defId tally, so a copy another card trashes out of your hand — or any opponent's copy — cannot tick a counter either. A per-instance counter is also ruled out: each copy is trashed at most once, and `selfCounter` under any key other than 'counter'/'uses'/'charges' sums `playCount` in alongside it (src/engine/effects/context.ts:148). The card is left in its safest state: the gate counts the copies you play, and the printed text now says exactly that rather than claiming the wider scope.

### chains_of_the_sovereign  [stillBroken]
The printed clause 'Unlocking it costs an opponent (-2) Prophet' cannot be made to work in card data, so it is left dormant (the safest state). Three engine gaps, all confirmed in the current tree: (a) Chains is PlayOnBuy + Flimsy and sits in `trash`, which DEFAULT_TRIGGER_ZONES (['play','hand','field'], src/engine/effects/triggers.ts:35) excludes — and widening the zones is exactly the regression just reverted, because (b) onUnlock is a non-self event fired with `subject: null` (src/engine/effects/ops/shop.ts:79), so it carries no pile identity and every Chains copy in the game answers every unlock of every pile; and (c) expireShopTimers (src/engine/core/turn.ts:41-62) sweeps expired locks and logs 'unlock' without ever calling fireEvent('onUnlock'), so the printed case — the lock Chains itself placed coming off naturally — raises no event at all. `Who` (src/engine/types.ts:273) is limited to self/eachOpponent/randomOpponent/chosenOpponent/eachPlayer, so the card cannot even charge the player who did the unlocking. Fixing it needs the engine to fire onUnlock from expireShopTimers and to pass the pile (and the unlocking player) with the event, plus a trigger condition that matches the pile this instance locked. The card text still prints the doc row (jlore_jlards_gameplay.md:660) verbatim; the clause is inert until that lands.

### shining_kit  [stillBroken]
Raised as an aside in the blood_diamond_cutter problem ('neither fixed nor listed as skipped'). The audit finding is still real and still not fixable in card data. Re-verified all three breaks: buff.ts:78-88 nextPlayed pushes {buffTimes, appliesTo:'play', uses:1} and drops `stat`; play.ts:173 accumulates mod.buffTimes into ResolvedMods and playCard never reads it (grep for buffTimes/buffStat returns only those writes plus types.ts:402); and nothing restricts the buff to a Resource. There is no card-data route to 'the next Resource you play': fireInstanceTriggers('onPlay', …) fires the triggers of the card being played, not of other cards sitting in play, so a trigger on Shining Kit cannot observe a later Resource. Left the card exactly as printed — the clause is inert but harmless (no wrong effect), the text stays faithful to the A.6 doc row, and it starts working the moment the engine consumes it. Engine fix needed (not my file): add buffStat?: StatKey to NextCardMod, set it in buff.ts's nextPlayed branch, and consume it in playCard after the stats step — `for (let k=0;k<mods.buffTimes;k++) applyBuff(s,'instance',iid,+1,mods.buffStat ?? null,rng)` plus the nerfTimes mirror. The same gap silently disables cn.ts:28 and food.ts:820/841. A card-data workaround (buff a Resource in hand instead) would change the printed behaviour, diverge from the doc row, and misfire onto Shining Kit itself when the hand holds no Resource, since runBuffNode falls back to item.sourceIid on an empty target — so I did not take it.

### advanced_refining  [stillBroken]
Root cause left in place, deliberately, since it is not my file: opTransform (effects/ops/movement.ts:189-190) still routes into:'upgrade'/'downgrade' to pickByCostDelta — a random card of that cost from the entire universe — and never calls upgradedDefId()/downgradedDefId() from src/engine/systems/upgrade.ts, which already implement the ladder including 'Diamond stays a Diamond' and 'Copper stays a Copper'. That is why Advanced Refining, Simple Refining and Pennymelting each have to spell the ladder out rung by rung, and why the choose-with-fallback shape is needed at all. If the engine is taught to resolve 'upgrade'/'downgrade' through the ladder when the source defId is on RESOURCE_LADDER, both cards collapse back to a single transform over filter:{defId:['copper','silver','gold']} with into:'upgrade', which gates itself for free. Cards work correctly as they stand; this is a simplification that is blocked on the engine.

### cup_runneth_over  [stillBroken]
Real, and not fixable in card data — the block is one layer deeper than the reviewer found. They named two card-data causes (a pool:{scope:'shop'} Discover samples defIds off instances actually in a pile via pools.ts zoneDefIds, so an empty pile offers none; and selectPilesWith drops zero-card piles inside its `if (sel && sel.filter)` branch at src/engine/effects/select.ts:378). Both confirmed. But there is a third, decisive one they missed: opReplenishPile's own empty-pile fallback at src/engine/effects/ops/shop.ts:173 reads `} else if (tryGetCard(pileId)) { defId = pileId; }` — and pile ids are `<shop>:<defId>` (makePileId, src/engine/shop/util.ts:21-23), never a bare card id, so tryGetCard('draft:foo') always returns null, defId stays null, and the loop `continue`s. An empty pile therefore cannot be replenished by ANY selector shape. The one-line engine fix is to use pileDefId(s, pileId) (src/engine/shop/util.ts:30-40), which already decodes the id and exists precisely so 'an emptied pile still knows what it was'; that file is not mine to edit.

Left in the safest state, deliberately unchanged. I evaluated swapping the Discover for a shared `pick:'shortest', count:1` on both nodes (with modifyCost ordered first, since it does not change pile heights, so the replenish re-derives the identical pile) — that reaches empty piles past both card-data causes. I rejected it: with the engine bug still present it makes the card strictly WORSE. Once any pile empties, 'shortest' locks onto it every time, the replenish silently no-ops, and the permanent -1 lands on an empty pile — a dead card. The current Discover always names a pile that has cards, so it always refills and always discounts something real. Doc line 881 ('Fully replenish a Draft pile') is satisfied for every partially-depleted pile; only the fully-empty case is unreachable.

I did add a comment on the node recording all three causes and the pileDefId fix, so a future editor does not re-litigate it. tsc clean, cards:validate OK, catalog-integrity 21/21.

### ricochet  [stillBroken]
Not fixable in card data. 'One Ricochet per turn' needs a player-scoped, turn-reset counter; the op set has only instance-scoped addCounter ({self:true} resolves to ctx.sourceIid per src/engine/effects/select.ts:244), and nothing in src/ reads 'ricochetUsedThisTurn' — PlayerState.counters' own doc comment (src/engine/types.ts:564) shows it was meant to live there. The NEW counter filter axis does not help: it filters selected cards, it cannot gate on a player counter, and even a per-instance read would not stop a DIFFERENT Ricochet card firing the same turn. Left in the safest state — the dead counter write is harmless and is retained as the hook for a future player-scoped counter or a resetTurnStats clear; the printed 'One Ricochet per turn' clause is kept so the card still matches the doc row rather than printing a permanently-unlimited effect.

### ricochet_plus  [stillBroken]
Identical to ricochet: the shared once-per-turn gate needs a player-scoped counter cleared in resetTurnStats (src/engine/core/turn.ts), which no op can write. Unchanged and left with its counter write and doc-faithful text.

### ricochet_plus_plus  [stillBroken]
Identical to ricochet: no player-scoped counter op exists, so the once-per-turn restriction cannot be enforced from card data. Unchanged and left with its counter write and doc-faithful text.

### what_is_love  [stillBroken]
The 'Fuse them' clause cannot be implemented in card data. There is no fusion op — `grep` for op:'fuse' over src/engine returns nothing, and src/engine/effects/ops/ holds no fusion handler — so the only reachable path is the fusedDefinition helper in @engine/systems, which nothing in the effect interpreter calls (discovery.ts's own fuseForCard wrapper has no importer). The card now takes both picks and creates one of them in hand, which matches the printed yield of one result per repeat but is a stand-in, not a merge. The audit's prescribed fix (audit-findings/advanced__discovery.md, what_is_love) is a new op plus a resume path that hands both picked defIds to it — engine work. The same gap sits on matchmaker, freaky_phil and frankenstein in this file, which the reviewer did not raise and I did not touch. SB-13 in docs/SOLVED-BLOCKERS.md fixes the fusion arithmetic, so the wiring is the only missing piece.

### oh_mr_lebon  [stillBroken]
Confirmed dead, and not fixable in card data. `libraryHeight > tallestOpponentLibrary` needs 'tallestOpponentLibrary' in EXPR_VARS (src/engine/types.ts:196) plus a line in buildVars (src/engine/effects/context.ts) computing max(opponent library lengths) — an engine file I do not own. No Condition can substitute: `Condition.has.atLeast` is typed `number`, never an Amount (types.ts:424), so a selector over `{who:'eachOpponent', zone:'library'}` can never be compared against libraryHeight, and countIn cannot address another player's zone (COUNT_ZONES in context.ts is your own hand/library/gy/play plus shop/trash). Verified live: with a 5-card Library against an emptied opponent Library, evalCondition returns false, so the card scores nothing today. Left in that fail-closed state deliberately and documented in a comment on the card — the pre-audit expression `max(0, libraryHeight - countIn(opponentLibrary, tallest))` collapsed to `max(0, libraryHeight)` and paid an unconditional, repeatable +2 VP on every play, which is the far more damaging direction. `text` still matches doc row 919; it becomes true the moment the variable lands, with no further card edit. Note the engine side is already landing variables of exactly this kind — 'longestCostRun' appeared in EXPR_VARS and buildVars mid-task and constellation in this same file was rewired to it by another agent.

### the_conglomerate  [stillBroken]
Confirmed dead, and not fixable in card data. `amount: {expr:'vpInHand'}` needs 'vpInHand' in EXPR_VARS + buildVars (summing statOf(inst,'vp') plus counters.vp over the hand, the same two terms core/scoring.ts:34 adds); no expression can express a VP *sum* today, since count()/countIn() count cards and CardFilter has no vp axis (only cost and the new counter axis, and a Points card's vp is a printed stat, not a counter). The one card-data approximation available — countIn(hand, points), i.e. +1 per Points card — is worse than nothing: it would pay +1 for every -1 VP card (Cursed Pig, Garlic, Chopped Chuzz), turning a hand of junk into positive VP, so I did not ship it. Verified live: playing the card with a Carat (1 VP) and a Master of Jlore (3 VP) in hand builds exactly one Skyscraper carrying vp counter 0 and still trashes both Points cards. The scoreOnCard target fix from the audit pass (pick:'bottom', so the score lands on the Skyscraper this play appended rather than an older one) is correct and stays. Documented in a comment on the card; `text` still matches doc row 922 and goes live unchanged once the variable exists.

### skull_of_juldan  [stillBroken]
The mechanical half of the doc row — 'for each DRAWN card costing (0)' — is not expressible in card data and stays deferred to the engine (the reviewer graded it [low], not damage). Nothing stamps instances that a draw just added, so a selector cannot name them. Every card-data workaround is worse than the approximation: it requires moving the printed +3 Cards out of `stats` into {op:'draw'} (against the stat-line convention) and pre-stamping the hand with {op:'addCounter'}, but opAddCounter falls back to the source instance when its selector matches nothing (src/engine/effects/ops/misc.ts:83) so an empty hand would stamp Skull itself, and the counter would persist on those instances across zones and turns with no reliable clear. Real fix is engine-side: stamp instances produced by drawCards (src/engine/core/zones.ts:310) for the length of the play, then filter on it via the new CardFilter.counter axis. Card left in the closest-legal state and the text now describes what it actually does.

### book_of_felinors  [stillBroken]
src/cards/tribes/books.ts:249-256. 'Each player discards 2 at random' cannot be expressed in card data: `discard` takes a Selector, and Selector `count` is a total across all matched players (src/engine/effects/select.ts:277-318 pools every player's zone, then shuffles and slices to `count`). No per-player discard op exists — `discardDownTo` loops per player but is 'down to N', not 'discard N'. The previous pass's split into a `who:'self'` node plus a `who:'eachOpponent'` node is the best card-local state and is correct for 2 players; with 3+ the opponents still lose 2 between them rather than 2 each. Left as-is (the audit's own note: 'There is no clean card-local workaround'). Needs `count` applied per resolved player in selectInstancesWith.

### book_of_blood  [stillBroken]
src/cards/tribes/books.ts:274-296. The doc row's second half — 'when trashed, add an original copy to their hand' on the marked cards — has no card-data expression. `setKeyword` only pushes to addedKeywords/removedKeywords, and fireEvent reads triggers off the definition only, so a marked instance cannot carry a rider. Left in its safest state: the `setKeyword` half works, and `text` states only what actually happens rather than promising the rider. Needs a counter honoured by trashWithTrigger in src/engine/effects/triggers.ts.

### felinor_factory  [stillBroken]
src/cards/archetypes/felinor.ts:265-275. 'If you trashed a Felinor this turn' is still `{has:{target:{zone:'trash', filter:{subtype:'Felinor'}}}}`, which is global and unscoped by turn — selectInstancesWith short-circuits the trash zone with zoneIds(state, null, 'trash') (select.ts:267-269), deliberately ignoring `who`. Since the `felinor` token is Flimsy and trashes on play, this is permanently true after any player plays one Felinor. EXPR_VARS (src/engine/types.ts:196-225) has no trashed-this-turn variable and no `who: 'activePlayer'` exists, so there is no card-data fix. Needs a per-player, per-turn trashed-Felinor counter plus an expr var. Same defect, same cause, on box_of_kitties (felinor.ts:294-300), whose clause is additionally forward-looking ('if you trash one this turn') and resolves before the player could have trashed anything.

### recurring_felinor  [stillBroken]
src/cards/archetypes/felinor.ts:22. 'It goes to the current player's GY instead' still resolves to the owner's GY. `moveTo` gained a `who`, but Who has no 'activePlayer' member (src/engine/types.ts:273 — 'self' | 'eachOpponent' | 'randomOpponent' | 'chosenOpponent' | 'eachPlayer'), and inside an onTrash trigger fireEvent has already overwritten item.player with `i.owner ?? actor`, so `who:'self'` resolves right back to the owner. Divergent only when another player's effect trashes it. Needs either an activePlayer Who or the trashing actor threaded through the onTrash context.
