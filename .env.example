# Upstash Redis — the entire backend. Free tier is plenty for 4 players.
# Create a database at https://console.upstash.com and copy the REST credentials.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: override the relay poll interval in ms (default 1000).
VITE_POLL_INTERVAL_MS=
